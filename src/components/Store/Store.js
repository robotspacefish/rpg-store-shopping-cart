import React from 'react';
import Inventory from '../Inventory/Inventory';

const Store = props => {
  return (
    <div className="Store">
      <Inventory />
    </div>
  );
};

export default Store;